import { NavLink, Outlet, useNavigate } from 'react-router-dom'; import { FileText, FolderOpen, LayoutDashboard, LogOut, Plus, ShieldCheck } from 'lucide-react'; import { useAuth } from '../context/AuthContext'; import Brand from '../components/Brand';
const nav = [[ '/', 'Dashboard', LayoutDashboard ], ['/documents', 'Documents', FileText], ['/organize', 'Categories & folders', FolderOpen]]; export default function AppLayout() { const { user, logout } = useAuth(); const navigate = useNavigate(); return <div className="shell"><aside><Brand/><div className="nav">{nav.map(([to, label, Icon]) => <NavLink end={to === '/'} key={to} to={to}><Icon size={18}/>{label}</NavLink>)}</div><div className="phase-note"><ShieldCheck size={17}/><span><b>Private by design</b><br/>Your files are only served after an owner check.</span></div><button className="sidebar-logout" onClick={() => { logout(); navigate('/login'); }}><LogOut size={17}/> Sign out</button></aside><main><header><div><p className="eyebrow">PERSONAL DOCUMENT VAULT</p><h1>Welcome back, {user?.name?.split(' ')[0]}</h1></div><div className="header-actions"><span className="planned">Notifications · Phase 2</span><button className="primary compact" onClick={() => navigate('/documents?upload=1')}><Plus size={18}/> Upload</button></div></header><Outlet/></main></div>; }
