import { useState } from 'react'; import { Navigate, useLocation, useNavigate } from 'react-router-dom'; import { Eye, EyeOff } from 'lucide-react'; import { useAuth } from '../context/AuthContext'; import Brand from '../components/Brand';
export default function AuthPage({ mode }) { const { user, auth } = useAuth(); const nav = useNavigate(); const location = useLocation(); const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const [show, setShow] = useState(false); if (user) return <Navigate to="/"/>; const submit = async (event) => { event.preventDefault(); setError(''); try { await auth(mode, form); nav(location.state?.from?.pathname || '/'); } catch (e) { setError(e.response?.data?.message || 'Unable to continue. Please try again.'); } }; return <div className="auth-page"><div className="auth-copy"><Brand/><p className="eyebrow">YOUR PRIVATE SPACE</p><h1>Keep life’s paperwork calm, close, and in your control.</h1><p>DocVault helps you keep important documents organized without losing sight of who can access them.</p><div className="trust">Built with protected routes, validated uploads, and private file access.</div></div><form className="auth-card" onSubmit={submit}><h2>{mode === 'login' ? 'Welcome back' : 'Create your vault'}</h2><p>{mode === 'login' ? 'Sign in to access your documents.' : 'Start organizing your important documents today.'}</p>{mode === 'register' && <label>Full name<input required minLength="2" value={form.name} onChange={e => setForm({ ...form, name: e.target.value })}/></label>}<label>Email address<input type="email" required value={form.email} onChange={e => setForm({ ...form, email: e.target.value })}/></label><label>Password<div className="password"><input type={show ? 'text' : 'password'} required minLength="10" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })}/><button type="button" onClick={() => setShow(!show)}>{show ? <EyeOff size={18}/> : <Eye size={18}/>}</button></div></label>{error && <div className="error">{error}</div>}<button className="primary wide">{mode === 'login' ? 'Sign in securely' : 'Create secure account'}</button><p className="switch">{mode === 'login' ? 'New to DocVault?' : 'Already have an account?'} <button type="button" onClick={() => nav(mode === 'login' ? '/register' : '/login')}>{mode === 'login' ? 'Create an account' : 'Sign in'}</button></p></form></div>; }
