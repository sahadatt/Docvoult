import jwt from 'jsonwebtoken'; import User from '../models/User.js'; import { httpError } from '../utils/httpError.js';
export const requireAuth = async (req, _res, next) => { try { const token = req.headers.authorization?.replace(/^Bearer\s+/i, ''); if (!token) throw httpError(401, 'Authentication is required.'); const payload = jwt.verify(token, process.env.JWT_SECRET); const user = await User.findById(payload.sub); if (!user) throw httpError(401, 'Your session is no longer valid.'); req.user = user; next(); } catch (error) { next(error.status ? error : httpError(401, 'Invalid or expired session.')); } };
